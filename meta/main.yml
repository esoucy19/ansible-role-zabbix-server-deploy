---
galaxy_info:
  author: Etienne Soucy
  description: Ansible role to deploy a fully functioning Zabbix server.
  company: your company (optional)

  # Specify issue tracker (default github)
  # issue_tracker_url: http://example.com/issue/tracker

  license: MIT

  min_ansible_version: 2.5

  # If container-enabled, specify minimum ansible container version.
  # min_ansible_container_version:

  # Specify github branch (default master)
  # github_branch:

  platforms:
    - name: EL
      versions:
        - 7

  galaxy_tags: []
dependencies:
  - bertvv.mariadb
  - dj-wasabi.zabbix-server
  - dj-wasabi.zabbix-web
